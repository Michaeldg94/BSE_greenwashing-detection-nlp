# Optional. Only needed to regenerate the GPT-4o / GPT-5.5 caches yourself.
# Copy this file to .env and set your key. Without it, the notebook runs from the bundled caches.
OPENAI_API_KEY=sk-your-key-here
